// ============================================
// Traits & Perks System
// ============================================

const Traits = (() => {
    function init() {
        console.log('Traits: Initializing...');
    }

    return {
        init
    };
})();

window.Traits = Traits;
