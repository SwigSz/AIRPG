// ============================================
// Quest Controller
// ============================================

const QuestManager = (() => {
    function init() {
        console.log('QuestManager: Initializing...');
    }

    return {
        init
    };
})();

window.QuestManager = QuestManager;
